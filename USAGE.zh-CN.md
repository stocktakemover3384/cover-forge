# CoverForge · 封面工坊 —— 使用说明

[English usage guide](USAGE.md) · [README（中文）](README.zh-CN.md)

CoverForge 只做一件事：把一张图裁成目标比例、缩放到目标像素、压到目标体积，
然后导出。全程在本机浏览器里完成，图片不上传、不联网。

这份说明按操作顺序写，遇到不确定的地方可以按小标题跳读。

**目录**

1. [五分钟上手](#1-五分钟上手)
2. [如何打开网页](#2-如何打开网页)
3. [如何上传图片](#3-如何上传图片)
4. [如何选择平台比例](#4-如何选择平台比例)
5. [如何设置目标文件大小](#5-如何设置目标文件大小)
6. [如何调整取景（裁剪位置）](#6-如何调整取景裁剪位置)
7. [如何选择输出尺寸](#7-如何选择输出尺寸)
8. [如何选择导出格式与背景色](#8-如何选择导出格式与背景色)
9. [如何理解「导出前信息」](#9-如何理解导出前信息)
10. [如何看懂四类提示](#10-如何看懂四类提示)
11. [如何下载与文件名规则](#11-如何下载与文件名规则)
12. [界面速查表](#12-界面速查表)
13. [常见问题 FAQ](#13-常见问题-faq)
14. [浏览器兼容](#14-浏览器兼容)
15. [诚实边界](#15-诚实边界)

---

## 1. 五分钟上手

1. 双击 `index.html`。
2. 把图片拖进页面（或点击上传区）。
3. 点一个平台预设卡片上的「套用」按钮（例如「Bilibili 视频封面 1920×1080」）。
4. 点「500 KB」。
5. 看右侧「导出前信息」：确认输出尺寸、预估文件大小、目标达成是不是你要的。
6. 点「下载 JPG · 1920 × 1080」。

第 3 步做完，比例和输出像素都定好了；第 4 步做完，体积也定好了。剩下的事工具会自己算。

---

## 2. 如何打开网页

**推荐：直接双击 `index.html`。**

- 不需要安装任何东西：没有依赖、没有构建、没有 Node、没有 npm。
- 不需要联网：页面只加载一个本地样式表、一个本地脚本和一个内联的 data URI 图标。
- `file://` 协议可直接使用。`app.js` 刻意写成经典 script + IIFE，因为 `file://` 下
  `<script type="module">` 会被 CORS 拦截 —— 这个工具不该要求你起服务器。
- 不需要把文件放到某个特定目录；整个 `cover-forge` 文件夹可以随意移动、复制，
  只要 `index.html`、`styles.css`、`app.js` 三个文件在同一层，就能用。
- 建议用 Chrome / Edge 打开（测试就是在这个环境里跑的），Safari 15+、Firefox 90+ 同样可用。

**可选：起一个本地静态服务器。** 不是必须的，只有在你想用 `http://` 调试时才需要：

```bash
cd cover-forge
python3 -m http.server 8000
# 然后打开 http://localhost:8000/
```

**升级 / 重装：** 直接用新版本的三个文件覆盖旧文件即可，没有缓存需要清（页面不用
Service Worker，也不写 localStorage）。如果浏览器缓存了旧文件，正常刷新一次即可；
必要时用 `Ctrl`+`Shift`+`R` / `⌘`+`Shift`+`R` 强制刷新。

---

## 3. 如何上传图片

三种方式，效果完全一样。

### 方式一：点击上传区

点击左侧「01 · 素材」面板里的上传区（写着「拖入图片」的大方块）。你也可以先用
`Tab` 把焦点移到它上面，再按 `Enter` 或空格。

载入完成后，这个按钮会变成「换一张图片」，随时可以换图。

### 方式二：拖拽到页面任意位置

把文件从访达 / 资源管理器拖到页面的**任何位置**（不是在某个小方框里），整个页面
会出现遮罩和「松开即可载入」，松手即载入。

- 拖多个文件时只取第一个。
- 拖到非图片文件会弹错误提示，告诉你支持哪些格式。
- 拖到图片但浏览器解不开（下载不完整、编码少见），会另有一条「图片解码失败」提示。

### 方式三：粘贴

按 `Ctrl`+`V`（macOS 为 `⌘`+`V`）。适合：

- 截图工具复制出来的图（macOS 的 `⇧`+`⌃`+`⌘`+`4` 截图后直接粘贴）；
- 在浏览器里右键复制的图片；
- 刚刚在别的软件里复制到剪贴板的图。

注意：光标停在输入框里时，粘贴会交给输入框，页面不会载入图片 —— 先点一下页面空白
处再粘贴。

### 支持的格式

| 格式 | 说明 |
|---|---|
| JPG / JPEG | 最常用，照片首选 |
| PNG | 支持透明通道 |
| WebP | 现代格式，浏览器原生支持 |
| GIF | **只载入其中一帧**，导出永远是静态图 |
| BMP | 需要浏览器能解码 |
| AVIF | 需要浏览器能解码（新版 Chrome / Safari 可以） |

载入新图片会**替换**当前图片，不保留历史。想从头开始（包括清空比例、体积等设置），
点右上角的「重置」。

### 载入后能看到什么

左侧面板会列出这张图的全部底细：

| 字段 | 含义 |
|---|---|
| 文件名 | 完整文件名 |
| 尺寸 | 原始像素尺寸，如 `1920 × 1080 px` |
| 像素量 | 百万像素数（MP），例如 `2.07 MP` |
| 比例 | 约分后的比例与小数，如 `16:9 · 1.778` |
| 文件大小 | 原始文件体积 |
| 格式 | JPEG / PNG / WEBP 等 |
| 透明通道 | 有 / 无 |

如果原图达到 **800 万像素**以上（例如 `4000 × 2000`、`3264 × 2448`），面板会提示
「大尺寸图片 · 处理可能需要数秒」。

---

## 4. 如何选择平台比例

比例决定「画面长什么样」，在右侧「02 · 画面比例」面板里选。

### 通用比例（10 个）

`16:9`、`9:16`、`1:1`、`3:4`、`4:3`、`4:5`、`5:4`、`2:3`、`3:2`、`21:9`。

点一下即生效，选中的会高亮。它只改比例，不动输出尺寸策略。

### 平台预设（7 个，分 5 组）

| 预设 | 比例 | 推荐像素 |
|---|---|---|
| Bilibili 视频封面 | 16:9 | 1920 × 1080 |
| YouTube 缩略图 | 16:9 | 1280 × 720 |
| 抖音 / 快手 / 视频号 | 9:16 | 1080 × 1920 |
| 小红书封面 | 3:4 | 1080 × 1440 |
| Instagram 帖子 | 4:5 | 1080 × 1350 |
| 方图 / 头像 | 1:1 | 1080 × 1080 |
| 公众号头图 | 2.35:1 | 1175 × 500 |

**卡片上「点哪里」结果不一样，这是最容易踩的一个点：**

| 点击位置 | 效果 |
|---|---|
| 点卡片本体（名字、比例那一块） | **只套用比例**。输出尺寸策略保持你原来的选择。 |
| 点卡片右侧的「套用」按钮 | **套用比例 + 把输出尺寸切到该平台的推荐像素**。 |

为什么会这样设计：很多人只是想要「16:9」这个构图，但不想被限制成 1080p；也有人
就是要平台要的那组像素。两种意图分成两个动作，避免你「只是想要个比例」却被悄悄
改成了 1080p。

点卡片本体之后，「04 · 输出尺寸」里的「平台推荐」选项会被点亮并显示该预设像素
（比如 `1920 × 1080`），但它**不会自动切过去** —— 你仍然留在原来的策略上。

### 自定义比例

在两个输入框里填 `宽 : 高`，然后按 `Enter` 或点「套用」。

- 支持小数，例如 `2.35 : 1`、`1.85 : 1`；
- 两个数值各自需要在 0 – 100 之间；
- 填 0 或负数会就地报错：「比例的两个数值都必须大于 0，例如 2.35 : 1。」；
- 超过 100 会提示「单个数值请控制在 0 – 100 之间。」。

### 一个需要注意的联动

**换比例会退回「最大可用」输出尺寸。** 因为原来那个平台的推荐像素（例如 1080×1920）
放在新比例下已经不成立了，继续沿用只会导出一个尺寸不匹配的文件。所以：先定比例，
再定输出尺寸，顺序更顺。

---

## 5. 如何设置目标文件大小

在右侧「03 · 目标文件大小」面板里设置。这一项是 CoverForge 的主要卖点 —— 平台对
封面体积往往有硬性上限（比如「不得超过 500 KB」），这里可以直接把它当输入。

### 快捷预设

`不限`、`200 KB`、`500 KB`、`1 MB`、`2 MB`，点一下即生效。

- **不限** = 不限体积，使用最高画质（JPEG 质量 0.92）。
- 其余选项会立刻触发压缩，尽量贴到目标以下。

### 自定义数值

1. 在「自定义目标」输入框里填数字；
2. 用右侧的 `KB` / `MB` 切换单位；
3. 按 `Enter` 或点「套用」。

补充几点行为：

- **输入后约 0.4 秒会自动生效**（不必非按回车），所以你连续输入数字时不会每敲一位
  就重算一次；按 `Enter` 或点「套用」则立即生效。
- **切换 KB / MB 时，已生效的目标会立刻按新单位重算**。所以「输入 2 → 点 MB」就等于
  2 MB。如果换算后超过 50 MB（例如先点「500 KB」再把单位切成 MB，就成了 500 MB），
  会立刻出现红色提示「目标不能超过 50 MB（当前 500 MB）。」，把数值改小后提示自动消失。
- 点快捷预设时，输入框和单位会跟着变：点「500 KB」→ 输入框显示 `500` + `KB`；点
  「2 MB」→ 输入框显示 `2` + `MB`。
- **留空 = 不限体积**，等价于点「不限」。
- **上限 50 MB**。填 60 MB 会报错：「目标不能超过 50 MB（当前 60 MB）。」。
- 数值必须大于 0，否则提示「请输入大于 0 的数值。」。

### 目标偏小时会发生什么

目标小于 **50 KB** 时，面板会给出「目标偏小，画质损失可能较明显。」的提示 —— 不会
拦你，只是先说实话。

### 压缩遵循的顺序

1. **先保分辨率，降质量**：固定分辨率，在 JPEG 质量 `0.40` – `0.95` 之间二分搜索
   「能达标的最高的那个质量」。质量的默认下限是 `0.40`。
2. **质量触底仍超标，才降分辨率**：按面积估算所需分辨率，降档后回到第 1 步重试
   （最多 6 轮）。

为什么是这个顺序：少几个像素比糊成一片好看，所以先牺牲质量、后牺牲分辨率。**分辨率
一旦被降过，导出前信息里的「分辨率变化」会写明**，例如
`原始分辨率 → 体积优化再降至 1600 × 900`。

3. **目标确实无法达到**：返回当前能做到的最小体积，并在「目标达成」里标「未达标」，
   脚注补一句「该目标过小，已压到当前可达的最小体积 …」。它不会假装成功。

---

## 6. 如何调整取景（裁剪位置）

CoverForge 永远按 **cover（填满）** 裁切：图片铺满整个画面，不变形、不留黑边。
换句话说是「裁掉多余的部分」，而不是「把整张图塞进去」。

默认**居中裁剪**。

### 先选一个预览视图

预览区右上角有一组视图切换：`成片` / `原图取景`。

| 视图 | 显示什么 | 什么时候用 |
|---|---|---|
| **成片**（默认） | 裁切并压缩后的结果，画面比例 = 输出比例。所见即所得地反映最终文件长什么样。 | 检查输出效果、整体构图。 |
| **原图取景** | **完整原图**（画面比例 = 原图比例），上面叠加一个裁剪框，框外压暗；框上角标显示输出像素（如 `1080 × 1080`）。 | 决定「保留原图的哪一块」、确认没有把关键内容裁掉。 |

**原图取景视图存在的意义**：只看成片时，你无法知道被裁掉的部分里有什么。切成原图
取景，就能一眼看到框外还有什么，再决定往哪个方向挪。这也是唯一能可靠避免「把人头
裁掉」的方式。

另外，点左侧流程里的「**05 取景**」会直接把预览切到「原图取景」并滚动到舞台 —— 因为
这一步真正的操作在舞台上，而不是右侧的说明面板。

### 拖动调整

在预览上按住拖动即可，鼠标、触屏、手写笔都支持（底层用指针事件）。

**两种视图的拖动语义是相反的，这是刻意设计的：**

| 视图 | 拖动时发生了什么 | 提示文案 |
|---|---|---|
| 成片 | 拖的是**画面**，裁剪窗反向移动（和大多数裁剪工具一致） | 「在框内拖动可调整取景焦点」 |
| 原图取景 | 拖的是**裁剪框**，框同向移动 | 「拖动裁剪框，决定保留原图的哪一块」 |

几点行为说明：

- 拖动只在「有余量」时有效。如果原图比例和目标比例刚好一致（比如原图就是 16:9，
  目标也是 16:9），没有可移动的空间，框会锁定，拖动提示会自动隐藏。
- 成片视图下拖动是即时重绘的（不重新编码），所以跟手；松手后才做正式编码。
- 原图取景视图下拖动会实时更新裁剪框位置和右侧的读数，同样不重新编码。
- 按 `Esc` 可以取消正在进行的拖动。

### 九宫格焦点按钮

预览下方「取景焦点」里有一组 3×3 按钮：左上、上中、右上、左中、正中、右中、左下、下中、右下。

点一下就跳到对应构图，例如想让主体靠上就点「上中」。这是拖动操作在键盘上的等价物。

右侧读数会显示当前焦点，例如 `焦点 50% / 20%`；居中时显示 `居中 · 50% / 50%`。

### 重置居中

预览右上角的「重置居中」，一键回到居中裁剪。

### 三分线开关

预览右上角的「三分线」按钮，开关构图辅助线。**只影响观看，不会导出到图里。**

在原图取景视图下，三分线会**跟随裁剪框**（只画在框内），因为铺满整张原图的三分线对
构图判断没有意义；成片视图下三分线铺满整个画面。

### 取景余量读数

右侧「05 · 取景」面板会给出三个数，用来判断你还有多少腾挪空间：

| 字段 | 含义 |
|---|---|
| 裁切区域 | 实际被裁出来的矩形，格式为 `宽 × 高 @ (x, y)` |
| 横向余量 | 左右两边一共可以移动多少像素（0 = 不能左右移） |
| 纵向余量 | 上下两边一共可以移动多少像素（0 = 不能上下移） |

---

## 7. 如何选择输出尺寸

这一项决定「导出多大的图」，在右侧「04 · 输出尺寸」面板里选。它和比例是两件事：
**比例决定形状，输出尺寸决定像素**。

| 策略 | 它做什么 | 适用场景 |
|---|---|---|
| **最大可用**（默认） | 输出 = 裁切区的全部像素，不主动降分辨率。 | 你要最高画质，没有像素要求。 |
| **平台推荐** | 用所套用平台的像素，如 `1920 × 1080`。 | 平台明确要求像素尺寸时。**先套用过平台预设，这一项才会亮起。** |
| **自定义长边** | 让**长边**等于你填的值（16 – 10000），另一边等比。 | 站内规范、CMS 限制、网页首图等。 |

「自定义长边」有 `1920`、`1280`、`1080`、`800` 四个快捷值，也可以直接输入任意数值
（例如 2400）。按 `Enter` 或改变输入框内容即生效；超出 16 – 10000 会提示
「长边像素需在 16 – 10000 之间。」。

### 两种方向的变化都会被说明

- **输出比裁切区大**（放大）：面板会给出放大提示，例如
  「将放大 4.80×（原始像素不足，多出的细节由插值生成，画质可能下降）」。
  放大的实现是逐级插值（每步最多 2×，高质量平滑），尽量减轻锯齿，但**它无法凭空
  造出细节**，成片一定比原图软。能选小一点的输出尺寸就选小一点。
- **输出比裁切区小**（缩小）：提示「已按你指定的输出尺寸缩小到原图可用分辨率的 xx%。」。

### 一个容易误解的地方

「最大可用」的意思是**不主动降分辨率**，但如果你**同时设了目标体积**、而这个体积在
全分辨率下达不到，压缩环节仍然会降分辨率（这是第 5 节说的第二步）。这种情况下
「分辨率变化」会写明「→ 体积优化再降至 …」。也就是说：

- 想要「只压体积、绝不降分辨率」：把目标设为**不限**，用质量滑块自己决定画质。
- 想要「严格贴住体积上限」：设目标体积，并接受可能的降分辨率。

---

## 8. 如何选择导出格式与背景色

在右侧「06 · 导出」面板里设置。

### 格式：JPG 还是 PNG

| | JPG | PNG |
|---|---|---|
| 体积 | 小（可压缩、可压到指定大小） | 大（无损，体积不可控） |
| 画质 | 有损，但 0.9 左右肉眼几乎无差 | 完全无损 |
| 透明 | 不支持，透明像素要被填成实色 | 支持，透明保留 |
| 适合 | 照片、实拍封面 | 截图、纯色块、带文字的图形、需要透明的场景 |

点击 `JPG` / `PNG` 切换。切换后预览和「导出前信息」会立刻重算。

### 背景色：只在「透明原图 + 导出 JPG」时出现

JPG 没有透明通道，透明像素必须变成某个实色。当工具检测到原图**含透明通道**且你选了
**JPG** 时，「填充背景色」会出现：

- 4 个快捷色板：白色、近黑、米白、品牌橙；
- 也可以点色块用取色器自选任意颜色；
- 预览会即时用这个颜色填充透明区域，所见即所得。

如果原图不含透明通道，这一项不会出现（因为没必要）。

### 手动覆盖 JPG 质量

「手动覆盖 JPG 质量」滑块默认在最左侧 = **自动**，意思是「质量由目标体积决定」。

往右拖就进入手动模式，面板右上角会显示百分比（例如 `85%`）。**手动指定质量会覆盖
目标体积** —— 此时目标体积不再约束文件大小，「目标达成」可能显示「未达标」。想回到
自动，把滑块拖回最左。

滑块只在 JPG 模式下出现（PNG 没有质量参数）。

---

## 9. 如何理解「导出前信息」

右侧最下方那一栏是导出前你要看的全部数据。面板顶部有一句说明值得先读一遍：
「下列数值全部取自同一份即将下载的文件 —— 预估文件大小不是估算，而是它的真实字节数。」

| 字段 | 含义 |
|---|---|
| 输出尺寸 | 即将导出的像素尺寸，如 `1920 × 1080 px` |
| 画面比例 | 你选的比例，以及它的十进制值（如 `16:9 · 1.778`） |
| 格式 | `JPG` 或 `PNG` |
| 目标体积 | 你的预算，未设置时显示 `不限（最高画质）` |
| 预估文件大小 | 即将下载的那份文件的大小 |
| 编码质量 | `xx%（自动匹配体积）`（设了目标体积）/ `xx%（固定高画质）`（目标为「不限」，即固定的 0.92）/ `xx%（手动指定）` / `无损（PNG）` |
| 分辨率变化 | `原始分辨率` / `放大 n×` / `缩小 n×`，被体积优化进一步降过会追加「→ 体积优化再降至 宽 × 高」 |
| 处理耗时 | 上一次渲染实际花了多久 |
| 目标达成 | `✅ 达标 · 用掉目标的 xx%` 或 `⚠ 未达标 · 超出 xx` |

### 为什么「预估文件大小」可信

因为这个数字**就是将要下载的那个文件的字节数**。工具不会先估一个再单独生成另一个：
面板读的就是最终 blob 的长度，点「下载」时也会等当前设置全部渲染完成后再取这份 blob。
所以面板显示多少，下载下来就是多少。面板顶部那句说明写的就是这件事。

唯一的差别是**显示精度**：小于 1 MB 时面板四舍五入到 0.1 KB，大于 1 MB 时保留 2 位
小数，所以显示值和精确字节数可能差不到 1 KB —— 文件本身完全一致。

### 「未达标」到底是什么意思

出现「⚠ 未达标」只有三种原因：

1. **格式决定的上限**：你选了 PNG 还设了目标体积。PNG 无损，没有任何参数能把体积压
   下去。面板会提供一键「改为 JPG 以达到目标大小」。
2. **目标太小，物理上做不到**：连最低质量、小到不能再小的分辨率都超标。工具会给你它
   能做到的最小文件，并明确标注，不会假装达标。
3. **你手动指定了质量**：手动质量优先于目标体积，此时体积不再被约束。

脚注还会补充其它细节：透明区域是否被填充成背景色、分辨率是否因为贴体积而被降过、
成片是否由放大生成。**这个脚注是刻意写的诚实说明，不是免责声明。**

---

## 10. 如何看懂四类提示

| 提示 | 出现位置 | 含义 | 你该做什么 |
|---|---|---|---|
| **放大提示** | 「04 · 输出尺寸」面板 + 「分辨率变化」 | 要求的输出比裁切区大，多出来的像素是插值生成的。提示会写明倍数，例如「将放大 4.80×」。 | 画质重要就把输出尺寸调小；确实需要那么大就接受它变软。 |
| **PNG 无损提示** | 「03 · 目标文件大小」面板（选 PNG 且设了目标时） | 「PNG 是无损格式，无法用质量参数控制体积。」 | 点「改为 JPG 以达到目标大小」，或者放弃体积目标。 |
| **画质风险提示** | 「03 · 目标文件大小」面板（目标 < 50 KB 时） | 「目标偏小，画质损失可能较明显。」 | 如果体积不是硬限制，把目标调大一点。 |
| **未达标警告** | 下载完成后的提示（toast），仅当下载的文件体积超过目标 | 标题「未达到目标体积」，正文写明实际体积、目标，并给出建议，例如「当前 46.9 KB，目标 20 KB。PNG 是无损格式，无法用质量参数压缩，可改用 JPG。」 | 换 JPG、把输出尺寸调小，或接受这个体积。 |

这几类提示都不会阻止你导出。它们存在的意义是：让你**在导出之前**（或者最迟刚下载完）
就知道，而不是上传之后才发现。

---

## 11. 如何下载与文件名规则

点中间的「下载封面」按钮。按钮标签上永远写着格式和像素，例如
`下载 JPG · 1920 × 1080`，所以点之前你已经知道会得到什么。

下载完成后右下角会弹一条成功提示，包含：文件名、最终体积、是否达标、处理耗时。

如果下载下来的文件**超出了目标体积**，还会紧接着多弹一条警告（例如「未达到目标体积 ·
当前 2.8 MB，目标 2 MB。可以尝试更小的输出尺寸，或改用 PNG 保留无损画质。」）。它不
影响下载，只是不让你带着「以为达标了」的错觉离开。

### 命名规则

```
<原文件名去掉扩展名>-<比例>-<宽>x<高>.<扩展名>
```

| 原文件 | 设置 | 导出文件名 |
|---|---|---|
| `my photo.jpg` | 16:9，1920×1080 | `my-photo-16x9-1920x1080.jpg` |
| `封面.png` | 2.35:1，1175×500 | `封面-2.35x1-1175x500.jpg` |
| `a/b:c.jpg` | 9:16，1080×1920 | `a-b-c-9x16-1080x1920.png` |

规则细节：

- 文件名里的空格、以及 `\ / : * ? " < > |` 这些系统不允许的字符会被替换成 `-`，
  连续多个 `-` 会合并，首尾的 `-` 会去掉；中文等其它字符原样保留。
- 比例标记用两位小数，`16:9` → `16x9`，`2.35:1` → `2.35x1`。
- 后缀按实际格式：JPG 为 `.jpg`，PNG 为 `.png`。
- 原始文件名不可用时回退为 `cover`。
- **注意**：文件名记录的是**目标比例**，不是实际输出比例。如果你用了「自定义长边」，
  宽高会按长边等比换算，可能和比例标记略有出入（例如 16:9 配长边 1000 → 输出
  `1000 × 563`，不是整数比例的精确值）。文件名里的宽高以实际像素为准。

浏览器下载目录取决于你的浏览器设置（Chrome 默认 `~/Downloads`）。

---

## 12. 界面速查表

页面分三栏（窄屏自动收成单列，顺序为：素材 → 预览 → 设置）。

| 位置 | 内容 |
|---|---|
| 顶栏 | 品牌、标语、`本地处理 · 不联网` 标记、「重置」 |
| 左栏 | 六步流程导航（点击可跳到对应面板）+ 「01 · 素材」面板 |
| 中间 | 「成片预览」+ 视图切换（`成片` / `原图取景`）+ 读数条 + 取景焦点九宫格 + 下载按钮 |
| 右栏 | 「02 · 画面比例」「03 · 目标文件大小」「04 · 输出尺寸」「05 · 取景」「06 · 导出」「导出前信息」 |
| 右下角 | 结果提示（成功 / 信息 / 警告 / 错误四类），可手动关闭，或几秒后自动消失 |

**关于右栏滚动**：桌面三栏布局（窗口宽度 ≥ 1280 px）下，右栏是**独立滚动**的 —— 它
的高度被限制在视口内，而六个面板加起来比它高得多，所以要到「06 · 导出」和「导出前
信息」，需要在**右栏内部**滚动（不是整页滚动）。窗口窄于 1280 px 时布局改版，右栏不再
独立滚动，变成整页滚动。

中间读数条上的状态标签会一直告诉你现在处于什么状态：

| 标签 | 含义 |
|---|---|
| 待载入 | 还没有图片 |
| 处理中 | 正在读取、裁切、重采样或编码 |
| 待处理 | 有图，但结果还没算出来 |
| 已就绪 | 未设置目标体积，成片可以导出 |
| 目标达成 | 设置了目标体积，且已达标 |
| 未达标 | 设置了目标体积，但没压到目标以下 |

### 快捷键

| 按键 | 作用 |
|---|---|
| `Ctrl`+`V` / `⌘`+`V` | 粘贴剪贴板里的图片 |
| `Tab` | 依次聚焦所有控件（焦点环可见） |
| `Enter` / `空格` | 在上传区上 = 打开文件选择框；在比例 / 体积输入框里 = 套用 |
| `Enter` | 在预设卡片上 = 只套用比例 |
| `Esc` | 关掉拖拽遮罩 / 取消正在进行的取景拖动 |

---

## 13. 常见问题 FAQ

**Q1：为什么 PNG 压不到我设的目标大小？**
PNG 是无损格式，没有质量参数可以用，能影响的只有像素数量。无损文件不可能想多小就多小。
如果体积是硬指标，请改用 JPG —— 面板里有现成的「改为 JPG 以达到目标大小」按钮。

**Q2：为什么压缩后不是我原来的分辨率？**
因为质量有下限。工具会先把 JPEG 质量从 0.95 往下降到 0.40 找「能达标的最高的质量」；
只有连 0.40 都还超标，才会降分辨率。这个顺序是刻意的：宁可少几个像素，也不要整张
糊满 JPEG 方块。降过分辨率时，「分辨率变化」里会写明 `→ 体积优化再降至 宽 × 高`。
如果你不希望被降分辨率，把目标设为「不限」。

**Q3：为什么放大之后画质变差了？**
放大无法创造细节。多出来的像素是插值算出来的，所以成片一定比原图软 —— 这是原理问题，
不是缺陷。想要清晰的封面，请用足够大的原图，或者把输出尺寸调小。

**Q4：拖进去没反应？**
按顺序排查：
1. 确认是图片格式（JPG / PNG / WebP / GIF / BMP）；非图片会弹一条说明支持格式的错误提示；
2. 确认松手时鼠标在页面范围内（拖动过程中页面会出现遮罩和「松开即可载入」）；
3. 如果文件确实能显示但这里报「图片解码失败」，说明文件被截断或用了浏览器不支持的编码，
   换一张或先转存一次；
4. 拖多个文件时只会载入第一个。

**Q5：粘贴没反应？**
1. 光标如果停在输入框里，粘贴会交给输入框。先点一下页面空白处再粘贴；
2. 确认剪贴板里真的有图片数据 —— 在文件管理器里「复制文件」通常只复制了**路径**，
   浏览器读不到图像。可以改用截图复制，或直接拖文件进页面。

**Q6：处理大图有点慢，正常吗？**
正常。设置目标体积后，为了找到「能达标的最高的质量」，工具会在多个候选分辨率上反复
编码（同一分辨率最多约十几次 JPEG 编码，最多试 6 个分辨率）。一张 1200 万像素的图
反复编码确实要几秒。处理期间预览上会盖一层「处理中」动画，页面不会卡死；「处理耗时」
会如实记录花费的时间。想更快：先把原图缩小，或用较小的输出尺寸。

**Q7：图片会被上传到服务器吗？**
不会。代码里根本没有任何上传路径：没有 `fetch`、没有 `XMLHttpRequest`、没有 WebSocket、
没有 Service Worker、没有外部资源。文件通过本地 File API 读取，像素处理全部在浏览器
自己的 Canvas 里完成。测试用例会断言「非 `file://` 的请求数为 0」。

**Q8：下载下来的文件大小和面板显示的数字为什么对不上？**
只会差一点点，而且只是**显示精度**：面板小于 1 MB 时四舍五入到 0.1 KB，超过 1 MB 保留
两位小数。面板读的就是最终文件的字节数，误差远小于 1 KB，文件内容完全一致。

**Q9：GIF 会保留动画吗？**
不会。原图只载入其中一帧，导出永远是静态图。

**Q10：导出 JPG 后，本该透明的区域变成黑色 / 没有背景色选项，怎么办？**
透明通道的检测基于 48×48 的降采样采样（任一点 alpha < 250 即判定为「有」）。绝大多数
含透明的图能被正确识别；但**面积非常小的透明区域在降采样时可能被平均掉**，于是显示为
「无」，背景色选项就不出现了，导出 JPG 时透明部分会按浏览器默认处理成黑色。遇到这种
情况，改导出 PNG（透明保留），或者先用别的工具给原图垫一层背景再处理。

**Q11：手机竖拍的照片方向不对？**
现代浏览器在解码 JPEG 时会自动应用 EXIF 方向信息，正常情况下手机竖拍照能正确显示。
如果确实遇到方向异常，请先在别的工具里把方向转正 —— CoverForge 不做旋转。

**Q12：能一次处理多张吗？**
当前版本不支持，一次处理一张。批量处理会让单图流程（也就是核心场景）的界面与状态
复杂度翻倍，所以刻意排到后续版本，见路线图。

**Q13：刷新页面后设置会保留吗？**
不会。所有状态只在当前页面内存里，刷新即回到初始状态（默认 16:9、不限体积、JPG）。
这是刻意的：不写入任何浏览器存储，也就不会留下任何本机痕迹。想重来一次直接点「重置」
即可，不需要刷新。

**Q14：下载时弹出「未达到目标体积」是什么意思？**
意思是这份文件比你要的目标大了。常见原因有三个：选了 PNG（无损，压不动）、目标定得
太小（已经压到质量下限还超标）、或者你手动指定了 JPG 质量（手动质量优先于目标体积）。
提示里会给建议（换 JPG / 把输出尺寸调小）。详见第 9 节「未达标到底是什么意思」。

**Q15：为什么我在预览里看不到被裁掉的部分？**
因为默认的「成片」视图只显示裁切后的结果。点预览右上角的「**原图取景**」，就能看到
完整原图 + 裁剪框，框外内容会压暗显示，还能直接拖动裁剪框调整。点左侧流程里的
「05 取景」也会自动帮你切过去。

---

## 14. 浏览器兼容

| 浏览器 | 情况 |
|---|---|
| **Chrome / Edge 90+** | 推荐环境，测试套件就跑在这里（43 项用例）。 |
| **Safari 15+** | 可用。依赖 `aspect-ratio`、CSS Grid、指针事件。JPEG 由系统编码器处理，最终字节数可能和 Chrome 略有差异。 |
| **Firefox 90+** | 可用。编码由平台实现，相同设置下输出可能和 Chrome 相差几个百分点。 |
| **移动浏览器** | 单列布局，取景用指针事件拖动。超大图可能触及手机内存上限。 |

**关于跨浏览器差异：** 各浏览器的 `canvas.toBlob` 背后是不同的图像编码器，所以同一个
质量参数在不同浏览器里产出的字节数**不会完全一致**。CoverForge 的压缩搜索每次都实测
当前浏览器的真实输出，因此无论在哪个浏览器，结果都会落在你的预算以内 —— 但「正好
500 KB 用掉 97%」这种具体数字可能略有不同。

---

## 15. 诚实边界

这些是工具的已知限制，写出来是为了让你少走弯路：

1. **PNG 的体积不可控**，无损就是无损。要体积就选 JPG。
2. **极端小的目标可能不可达**，此时会给出最小可达体积并标注「未达标」，不会假装成功。
3. **放大不能增加细节**，只会插值，成片会变软。
4. **动图只取一帧**，导出永远是静态图。
5. **画质损失不可逆**：导出后会重新编码，请保留原图。
6. **透明通道检测是采样判定**（48×48），极小的透明区域可能被判为「无」，见 Q10。
7. **分辨率上限**：输出宽高会被限制在 12000 px 以内；「自定义长边」的取值范围是
   16 – 10000 px。
8. **一次一张**，没有批量。
9. **不做旋转、不做调色**：CoverForge 只负责比例、尺寸、体积三件事，亮度、对比度、
   滤镜请交给别的工具。
10. **不写入任何本地存储**，刷新即重置。
